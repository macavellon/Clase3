Para el repositorio:
    Primero inicializo en gitbush el repositorio, luego voy a github y agrego un  nuevo repositorio
    Para unirlos entre si tengo que poner : git remote add origin yacaellinkdegithub
    Luego  pongo git add . para avisar que voy a agregar cambios al repositorio de Github
     Luego hago: git commit -m ' Nombre  del commit'
     ahi voy a subir los cambios  del  local al remoto
     luego pongo git push -u origin main para cargar todo 
     la primera  vez que lo  hago  puedo poner git push --set-upstream origin main
     luego puedo poner  solo git push